import 'package:flutter/material.dart';

const red = Color(0xFF8C031C);

const green = Color(0xFF93A603);

const greenb = Color(0xFF667302);

const yellow = Color.fromARGB(255, 242, 183, 5);

const yellow50 = Color.fromARGB(118, 242, 183, 5);

const brown = Color(0xFF8C2703);

const brown75 = Color.fromARGB(200, 140, 39, 3);

const white = Color.fromARGB(255, 255, 255, 255);

const white25 = Color.fromARGB(75, 255, 255, 255);

const color2Selected = Color.fromARGB(255, 0, 34, 48);

const color2 = Color.fromARGB(255, 2, 64, 89);

const color2light = Color.fromARGB(180, 2, 64, 89);

const color2Selectedlight = Color.fromARGB(180, 0, 34, 48);

TextStyle whiteTextStyle(double size) {
  return TextStyle(
    color: white,
    fontFamily: "Unbounded",
    fontSize: size,
  );
}

TextStyle blackTextStyle(double size) {
  return TextStyle(
    color: white,
    fontFamily: "Unbounded",
    fontSize: size,
  );
}

TextStyle lowWhiteTextStyle(double size) {
  return TextStyle(
    color: white,
    fontFamily: "Unbounded",
    fontSize: size,
  );
}

TextStyle lowBlackTextStyle(double size) {
  return TextStyle(
    color: white,
    fontFamily: "Unbounded",
    fontSize: size,
  );
}
